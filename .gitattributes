*.img filter=lfs diff=lfs merge=lfs -text
*.efi filter=lfs diff=lfs merge=lfs -text
vmlinuz-alibaba-docker filter=lfs diff=lfs merge=lfs -text
# disk-alibaba.raw is published as a plain git blob (not LFS) so the build
# instance can fetch it token-free via `git fetch`/`pull`.
disk-alibaba.raw -text
